{
  "name": "external-form-sample",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "description": "Expense UI: React (Vite) app in web/; legacy static HTML remains in public/ for reference.",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web"
  },
  "dependencies": {}
}
