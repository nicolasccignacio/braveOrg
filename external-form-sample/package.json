{
  "name": "external-form-sample",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "description": "Static expense UI (localStorage) and browser-only bank PDF import; deployable to GitHub Pages without a backend.",
  "dependencies": {}
}
